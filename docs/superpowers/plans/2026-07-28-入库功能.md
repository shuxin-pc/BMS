# 入库功能实施计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现入库功能完整闭环：后端新增 `/inbound` 语义化端点（自动维护库存与批次）、补齐 `InventoryLogDto` 显示字段、新增商品/供应商轻量 `/options` 端点；前端替换 Mock API 为真实 HTTP 调用。

**Architecture:** 后端在 `Bms.Store.Application` 新增 `InboundAppService`（事务内写 `InventoryLog` + `InventoryBatch` + `Inventory` 三表），保留现有通用 `POST /InventoryLogs` 端点不动。前端重写 `src/api/inventory-ops/index.ts`，删除 Mock 数据，6 个函数改调真实后端端点。`inbound/index.vue` 页面组件不动。

**Tech Stack:** .NET 8 / EF Core / Mapster / FluentValidation / Vue 3 / TypeScript / Element Plus

**Spec:** `docs/superpowers/specs/2026-07-28-入库功能-design.md`

**构建命令：**
- 后端：`dotnet build Bms.sln`
- 前端：`cd src/Admin/mes-admin && npm run build`
- EF Migration：`dotnet ef migrations add <Name> --project src/Services/Store/Bms.Store.Infrastructure --startup-project src/Services/Store/Bms.Store.Api`

---

## Chunk 1: 后端数据模型与 DTO

### Task 1: InventoryLog 实体新增 OperatorName 字段与 Supplier 导航属性

**Files:**
- Modify: `src/Services/Store/Bms.Store.Domain/Entities/InventoryLog.cs`

- [ ] **Step 1: 在 `InventoryLog` 实体末尾（`Product` 导航属性之前）追加 `OperatorName` 字段与 `Supplier` 导航属性**

在 `src/Services/Store/Bms.Store.Domain/Entities/InventoryLog.cs` 的 `Remark` 属性之后、`Product` 导航属性之前追加：

```csharp
    /// <summary>
    /// 操作人姓名（冗余存储，写入时取 ICurrentUser.RealName ?? UserName）
    /// </summary>
    public string? OperatorName { get; set; }
```

在 `Product` 导航属性之后追加：

```csharp
    /// <summary>
    /// 导航属性：供应商
    /// </summary>
    public Supplier? Supplier { get; set; }
```

- [ ] **Step 2: 验证编译**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED（无错误）

- [ ] **Step 3: Commit**

```bash
git add src/Services/Store/Bms.Store.Domain/Entities/InventoryLog.cs
git commit -m "feat: InventoryLog 实体新增 OperatorName 字段与 Supplier 导航属性"
```

---

### Task 2: StoreDbContext 配置 Supplier 导航属性外键

**Files:**
- Modify: `src/Services/Store/Bms.Store.Infrastructure/StoreDbContext.cs`

- [ ] **Step 1: 查找 `InventoryLog` 实体配置位置**

Run: `grep -n "InventoryLog" src/Services/Store/Bms.Store.Infrastructure/StoreDbContext.cs`

找到 `InventoryLog` 的 `HasOne`/`HasMany` 或 `Entity<InventoryLog>()` 配置块。若无显式配置，则需新增。

- [ ] **Step 2: 在 `OnModelCreating` 中为 `InventoryLog` 配置 `Supplier` 导航属性外键**

在 `StoreDbContext.OnModelCreating` 中找到 `Entity<InventoryLog>()` 配置块（若无则新增），追加：

```csharp
            modelBuilder.Entity<InventoryLog>(entity =>
            {
                // 现有配置保留...

                // Supplier 导航属性外键
                entity.HasOne(l => l.Supplier)
                      .WithMany()
                      .HasForeignKey(l => l.SupplierId)
                      .OnDelete(DeleteBehavior.Restrict)
                      .IsRequired(false);
            });
```

注：`OnDelete(DeleteBehavior.Restrict)` 避免供应商删除时级联删除流水记录。

- [ ] **Step 3: 验证编译**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED

- [ ] **Step 4: Commit**

```bash
git add src/Services/Store/Bms.Store.Infrastructure/StoreDbContext.cs
git commit -m "feat: StoreDbContext 配置 InventoryLog.Supplier 导航属性外键"
```

---

### Task 3: 创建 EF Core migration

**Files:**
- Create: `src/Services/Store/Bms.Store.Infrastructure/Migrations/<timestamp>_AddInventoryLogOperatorName.cs`
- Create: `src/Services/Store/Bms.Store.Infrastructure/Migrations/<timestamp>_AddInventoryLogOperatorName.Designer.cs`
- Modify: `src/Services/Store/Bms.Store.Infrastructure/Migrations/StoreDbContextModelSnapshot.cs`

- [ ] **Step 1: 生成 migration**

Run:
```bash
dotnet ef migrations add AddInventoryLogOperatorName --project src/Services/Store/Bms.Store.Infrastructure --startup-project src/Services/Store/Bms.Store.Api
```
Expected: 生成新 migration 文件，包含 `AddColumn<string>("OperatorName", ...)` 操作。`SupplierId` 列已存在，migration 不应包含新增 `SupplierId` 列。

- [ ] **Step 2: 检查生成的 migration 文件**

打开生成的 `*_AddInventoryLogOperatorName.cs`，确认 `Up` 方法包含：
```csharp
migrationBuilder.AddColumn<string>(
    name: "OperatorName",
    table: "InventoryLogs",
    type: "...",
    nullable: true);
```

- [ ] **Step 3: 验证编译**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED

- [ ] **Step 4: Commit**

```bash
git add src/Services/Store/Bms.Store.Infrastructure/Migrations/
git commit -m "feat: 添加 AddInventoryLogOperatorName migration"
```

---

### Task 4: InventoryLogDto 补显示字段

**Files:**
- Modify: `src/Services/Store/Bms.Store.Application/Dtos/Inventories/InventoryLogDto.cs`

- [ ] **Step 1: 在 `InventoryLogDto` 末尾追加显示字段**

在 `src/Services/Store/Bms.Store.Application/Dtos/Inventories/InventoryLogDto.cs` 的 `UpdatedAt` 属性之后追加：

```csharp
    /// <summary>商品名称（查询时 JOIN 填充）</summary>
    public string? ProductName { get; set; }

    /// <summary>商品编码（查询时 JOIN 填充）</summary>
    public string? ProductCode { get; set; }

    /// <summary>供应商名称（查询时 JOIN 填充）</summary>
    public string? SupplierName { get; set; }

    /// <summary>操作人姓名（写入时冗余存储）</summary>
    public string? OperatorName { get; set; }
```

- [ ] **Step 2: 验证编译**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED

- [ ] **Step 3: Commit**

```bash
git add src/Services/Store/Bms.Store.Application/Dtos/Inventories/InventoryLogDto.cs
git commit -m "feat: InventoryLogDto 补充商品/供应商/操作人显示字段"
```

---

### Task 5: InventoryLogQueryDto 补查询字段

**Files:**
- Modify: `src/Services/Store/Bms.Store.Application/Dtos/Inventories/InventoryLogQueryDto.cs`

- [ ] **Step 1: 在 `InventoryLogQueryDto` 末尾追加查询字段**

在 `src/Services/Store/Bms.Store.Application/Dtos/Inventories/InventoryLogQueryDto.cs` 的 `SourceType` 属性之后追加：

```csharp
    /// <summary>商品名称（模糊匹配）</summary>
    public string? ProductName { get; set; }

    /// <summary>开始日期（CreatedTime >= StartDate）</summary>
    public DateTime? StartDate { get; set; }

    /// <summary>结束日期（CreatedTime <= EndDate，含当日）</summary>
    public DateTime? EndDate { get; set; }
```

注：现有 `ProductId` 过滤器保留不动。

- [ ] **Step 2: 验证编译**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED

- [ ] **Step 3: Commit**

```bash
git add src/Services/Store/Bms.Store.Application/Dtos/Inventories/InventoryLogQueryDto.cs
git commit -m "feat: InventoryLogQueryDto 补充 ProductName/StartDate/EndDate 查询字段"
```

---

### Task 6: 创建 InboundCreateDto

**Files:**
- Create: `src/Services/Store/Bms.Store.Application/Dtos/Inventories/InboundCreateDto.cs`

- [ ] **Step 1: 创建 DTO 文件**

创建 `src/Services/Store/Bms.Store.Application/Dtos/Inventories/InboundCreateDto.cs`：

```csharp
namespace Bms.Store.Application.Dtos.Inventories;

/// <summary>
/// 入库请求 DTO（对应 POST /api/store/InventoryLogs/inbound）
/// </summary>
public class InboundCreateDto
{
    /// <summary>商品ID</summary>
    public long ProductId { get; set; }

    /// <summary>
    /// 入库来源类型，取值见 InventoryLogSourceTypes：
    /// 1=采购入库 2=退货入库 3=盘点调整 4=调拨入库 6=其他
    /// 非入库来源 {0,5,7,8,9,10,11} 由 Validator 拒绝
    /// </summary>
    public int SourceType { get; set; }

    /// <summary>供应商ID（SourceType=1 采购入库时必填）</summary>
    public long? SupplierId { get; set; }

    /// <summary>入库数量（>0）</summary>
    public decimal Quantity { get; set; }

    /// <summary>单价（用于 FIFO 成本计算）</summary>
    public decimal? UnitPrice { get; set; }

    /// <summary>批次号（仅用于内部追溯，不参与扣减排序）</summary>
    public string? BatchNo { get; set; }

    /// <summary>生产日期（仅写入 InventoryBatch）</summary>
    public DateTime? ProductionDate { get; set; }

    /// <summary>保质期天数（仅写入 InventoryBatch）</summary>
    public int? ShelfLifeDays { get; set; }

    /// <summary>过期日期；未填视为"无效期限制"批次</summary>
    public DateTime? ExpirationDate { get; set; }

    /// <summary>备注（仅写入 InventoryLog）</summary>
    public string? Remark { get; set; }
}
```

- [ ] **Step 2: 验证编译**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED

- [ ] **Step 3: Commit**

```bash
git add src/Services/Store/Bms.Store.Application/Dtos/Inventories/InboundCreateDto.cs
git commit -m "feat: 创建 InboundCreateDto 入库请求 DTO"
```

---

### Task 7: 创建 InboundCreateDtoValidator

**Files:**
- Create: `src/Services/Store/Bms.Store.Application/Validators/InboundCreateDtoValidator.cs`

- [ ] **Step 1: 创建 Validator 文件**

创建 `src/Services/Store/Bms.Store.Application/Validators/InboundCreateDtoValidator.cs`：

```csharp
using FluentValidation;
using Bms.Store.Application.Dtos.Inventories;
using Bms.Store.Domain.Entities;

namespace Bms.Store.Application.Validators;

/// <summary>
/// 入库请求 DTO 验证规则
/// </summary>
public class InboundCreateDtoValidator : AbstractValidator<InboundCreateDto>
{
    /// <summary>合法的入库来源类型集合</summary>
    private static readonly HashSet<int> ValidInboundSourceTypes = new()
    {
        InventoryLogSourceTypes.PurchaseInbound,    // 1
        InventoryLogSourceTypes.ReturnInbound,      // 2
        InventoryLogSourceTypes.CheckAdjustment,    // 3
        InventoryLogSourceTypes.TransferInbound,    // 4
        InventoryLogSourceTypes.Other               // 6
    };

    public InboundCreateDtoValidator()
    {
        RuleFor(x => x.ProductId)
            .GreaterThan(0).WithMessage("商品ID无效");

        RuleFor(x => x.SourceType)
            .Must(st => ValidInboundSourceTypes.Contains(st))
            .WithMessage("入库来源类型不合法，仅允许 1=采购 2=退货 3=盘点 4=调拨 6=其他");

        RuleFor(x => x.SupplierId)
            .NotNull().When(x => x.SourceType == InventoryLogSourceTypes.PurchaseInbound)
            .WithMessage("采购入库必须选择供应商");

        RuleFor(x => x.Quantity)
            .GreaterThan(0).WithMessage("入库数量必须大于0");

        RuleFor(x => x)
            .Must(x => !x.ProductionDate.HasValue || !x.ExpirationDate.HasValue
                       || x.ExpirationDate > x.ProductionDate)
            .WithMessage("过期日期不能早于生产日期");
    }
}
```

- [ ] **Step 2: 验证编译**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED

- [ ] **Step 3: Commit**

```bash
git add src/Services/Store/Bms.Store.Application/Validators/InboundCreateDtoValidator.cs
git commit -m "feat: 创建 InboundCreateDtoValidator 验证规则"
```

---

## Chunk 2: 后端 AppService 与 Controller

### Task 8: 创建 IInboundAppService 接口

**Files:**
- Create: `src/Services/Store/Bms.Store.Application/Services/IInboundAppService.cs`

- [ ] **Step 1: 创建接口文件**

创建 `src/Services/Store/Bms.Store.Application/Services/IInboundAppService.cs`：

```csharp
using Bms.Store.Application.Dtos;
using Bms.Store.Application.Dtos.Inventories;

namespace Bms.Store.Application.Services;

/// <summary>
/// 入库应用服务接口
/// 负责语义化入库端点的业务逻辑：事务内写 InventoryLog + InventoryBatch + Inventory 三表
/// </summary>
public interface IInboundAppService
{
    /// <summary>
    /// 创建入库记录（事务内同步维护批次表与库存汇总表）
    /// </summary>
    /// <param name="dto">入库请求 DTO</param>
    /// <returns>带显示字段的库存流水 DTO</returns>
    Task<ApiResponseDto<InventoryLogDto>> CreateAsync(InboundCreateDto dto);
}
```

- [ ] **Step 2: 验证编译**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED

- [ ] **Step 3: Commit**

```bash
git add src/Services/Store/Bms.Store.Application/Services/IInboundAppService.cs
git commit -m "feat: 创建 IInboundAppService 入库服务接口"
```

---

### Task 9: 创建 InboundAppService 实现

**Files:**
- Create: `src/Services/Store/Bms.Store.Application/Services/InboundAppService.cs`

- [ ] **Step 1: 创建服务实现文件**

创建 `src/Services/Store/Bms.Store.Application/Services/InboundAppService.cs`：

```csharp
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.Logging;
using FluentValidation;
using Bms.BuildingBlocks.Abstractions.Security;
using Bms.Store.Application.Dtos;
using Bms.Store.Application.Dtos.Inventories;
using Bms.Store.Domain.Entities;
using Bms.Store.Infrastructure;

namespace Bms.Store.Application.Services;

/// <summary>
/// 入库应用服务实现
/// 事务内写 InventoryLog + InventoryBatch + Inventory 三表，自动维护库存与批次
/// </summary>
public class InboundAppService : IInboundAppService
{
    private readonly StoreDbContext _dbContext;
    private readonly ICurrentUser _currentUser;
    private readonly IValidator<InboundCreateDto> _validator;
    private readonly ILogger<InboundAppService> _logger;

    public InboundAppService(
        StoreDbContext dbContext,
        ICurrentUser currentUser,
        IValidator<InboundCreateDto> validator,
        ILogger<InboundAppService> logger)
    {
        _dbContext = dbContext;
        _currentUser = currentUser;
        _validator = validator;
        _logger = logger;
    }

    /// <summary>
    /// 创建入库记录
    /// 事务边界：全程在 IDbContextTransaction 内执行，三表写入要么全部成功要么全部回滚
    /// </summary>
    public async Task<ApiResponseDto<InventoryLogDto>> CreateAsync(InboundCreateDto dto)
    {
        if (!_currentUser.TenantId.HasValue || !_currentUser.StoreId.HasValue)
            return ApiResponseDto<InventoryLogDto>.Fail("无法确定当前租户或门店", 401);

        var validation = await _validator.ValidateAsync(dto);
        if (!validation.IsValid)
            return ApiResponseDto<InventoryLogDto>.Fail(string.Join("; ", validation.Errors.Select(e => e.ErrorMessage)), 400);

        var tenantId = _currentUser.TenantId.Value;
        var storeId = _currentUser.StoreId.Value;

        await using var transaction = await _dbContext.Database.BeginTransactionAsync();
        try
        {
            // 1. 校验商品存在且属于当前门店
            var product = await _dbContext.Products
                .FirstOrDefaultAsync(p => p.Id == dto.ProductId
                    && p.TenantId == tenantId
                    && p.StoreId == storeId
                    && !p.IsDeleted);
            if (product == null)
                return ApiResponseDto<InventoryLogDto>.Fail("商品不存在", 400);

            // 2. 采购入库校验供应商存在
            if (dto.SourceType == InventoryLogSourceTypes.PurchaseInbound)
            {
                if (!dto.SupplierId.HasValue)
                    return ApiResponseDto<InventoryLogDto>.Fail("采购入库必须选择供应商", 400);

                var supplierExists = await _dbContext.Suppliers
                    .AnyAsync(s => s.Id == dto.SupplierId.Value
                        && s.TenantId == tenantId
                        && s.StoreId == storeId
                        && !s.IsDeleted);
                if (!supplierExists)
                    return ApiResponseDto<InventoryLogDto>.Fail("供应商不存在", 400);
            }

            // 3. 查当前库存汇总记录，计算 BeforeQuantity / AfterQuantity
            var inventory = await _dbContext.Inventories
                .FirstOrDefaultAsync(i => i.ProductId == dto.ProductId
                    && i.TenantId == tenantId
                    && i.StoreId == storeId);
            var beforeQuantity = inventory?.Quantity ?? 0m;
            var afterQuantity = beforeQuantity + dto.Quantity;

            // 4. 写 InventoryLog（Type=1 入库）
            var log = new InventoryLog
            {
                ProductId = dto.ProductId,
                Type = 1,
                SourceType = dto.SourceType,
                SupplierId = dto.SupplierId,
                UnitPrice = dto.UnitPrice,
                Quantity = dto.Quantity,
                BeforeQuantity = beforeQuantity,
                AfterQuantity = afterQuantity,
                BatchNo = dto.BatchNo,
                ExpirationDate = dto.ExpirationDate,
                Remark = dto.Remark,
                OperatorName = _currentUser.RealName ?? _currentUser.UserName,
                TenantId = tenantId,
                TenantCode = _currentUser.TenantCode ?? string.Empty,
                StoreId = storeId,
                StoreCode = _currentUser.StoreCode ?? string.Empty,
                CreatedTime = DateTime.Now
            };
            _dbContext.InventoryLogs.Add(log);

            // 5. 写 InventoryBatch（始终新建，不累加、不覆盖，避免破坏 FEFO 排序）
            var batch = new InventoryBatch
            {
                ProductId = dto.ProductId,
                BatchNo = dto.BatchNo ?? string.Empty,
                Quantity = dto.Quantity,
                UnitPrice = dto.UnitPrice ?? 0m,
                ProductionDate = dto.ProductionDate,
                ShelfLifeDays = dto.ShelfLifeDays,
                ExpirationDate = dto.ExpirationDate,
                PurchaseDate = DateTime.Today,
                Status = 1,
                TenantId = tenantId,
                TenantCode = _currentUser.TenantCode ?? string.Empty,
                StoreId = storeId,
                StoreCode = _currentUser.StoreCode ?? string.Empty,
                CreatedTime = DateTime.Now
            };
            _dbContext.InventoryBatches.Add(batch);

            // 6. 更新 Inventory 汇总表（不存在则插入，存在则累加）
            if (inventory == null)
            {
                _dbContext.Inventories.Add(new Inventory
                {
                    ProductId = dto.ProductId,
                    Quantity = dto.Quantity,
                    AlertQuantity = 0m,
                    TenantId = tenantId,
                    TenantCode = _currentUser.TenantCode ?? string.Empty,
                    StoreId = storeId,
                    StoreCode = _currentUser.StoreCode ?? string.Empty,
                    CreatedTime = DateTime.Now
                });
            }
            else
            {
                inventory.Quantity = afterQuantity;
                inventory.UpdatedTime = DateTime.Now;
            }

            await _dbContext.SaveChangesAsync();
            await transaction.CommitAsync();
        }
        catch (DbUpdateException ex)
        {
            await transaction.RollbackAsync();
            _logger.LogError(ex, "入库失败：ProductId={ProductId}, SourceType={SourceType}", dto.ProductId, dto.SourceType);
            return ApiResponseDto<InventoryLogDto>.Fail("入库失败，请重试", 500);
        }
        catch (Exception ex)
        {
            await transaction.RollbackAsync();
            _logger.LogError(ex, "入库异常：ProductId={ProductId}, SourceType={SourceType}", dto.ProductId, dto.SourceType);
            return ApiResponseDto<InventoryLogDto>.Fail("入库失败，请重试", 500);
        }

        // 事务已提交，重新查询填充显示字段（移出 try 块：若此查询失败，数据已落库，
        // 不应回滚已提交事务或返回 500 让用户误重试导致重复入库）
        var logWithNav = await _dbContext.InventoryLogs
            .Include(l => l.Product)
            .Include(l => l.Supplier)
            .FirstAsync(l => l.Id == log.Id);

        var result = new InventoryLogDto
        {
            Id = logWithNav.Id,
            ProductId = logWithNav.ProductId,
            Type = logWithNav.Type,
            SourceType = logWithNav.SourceType,
            SupplierId = logWithNav.SupplierId,
            UnitPrice = logWithNav.UnitPrice,
            Quantity = logWithNav.Quantity,
            BeforeQuantity = logWithNav.BeforeQuantity,
            AfterQuantity = logWithNav.AfterQuantity,
            BatchNo = logWithNav.BatchNo,
            ExpirationDate = logWithNav.ExpirationDate,
            RelatedId = logWithNav.RelatedId,
            Remark = logWithNav.Remark,
            CreatedAt = logWithNav.CreatedTime,
            UpdatedAt = logWithNav.UpdatedTime,
            ProductName = logWithNav.Product?.Name,
            ProductCode = logWithNav.Product?.Code,
            SupplierName = logWithNav.Supplier?.Name,
            OperatorName = logWithNav.OperatorName
        };
        return ApiResponseDto<InventoryLogDto>.Ok(result, "入库成功");
    }
}
```

**关键设计点说明：**
- 事务边界：`BeginTransactionAsync` 包裹三表写入，异常 `RollbackAsync`
- 批次写入策略：始终 `Add` 新记录，不查旧批次，不累加、不覆盖（依据 spec §5 批次写入策略）
- `OperatorName`：取 `ICurrentUser.RealName ?? UserName`，冗余存储避免跨服务查 System 用户表
- `PurchaseDate`：后端自动设为 `DateTime.Today`（前端表单无此字段）
- 显示字段填充：提交事务后重新 `Include` 查询一次，填充 `ProductName/ProductCode/SupplierName/OperatorName`

- [ ] **Step 2: 验证编译**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED

- [ ] **Step 3: Commit**

```bash
git add src/Services/Store/Bms.Store.Application/Services/InboundAppService.cs
git commit -m "feat: 创建 InboundAppService 入库服务实现（事务内三表写入）"
```

---

### Task 10: 注册 IInboundAppService

**Files:**
- Modify: `src/Services/Store/Bms.Store.Application/Extensions/ServiceCollectionExtensions.cs`

- [ ] **Step 1: 在 `AddApplicationServices` 方法中注册 `IInboundAppService`**

在 `src/Services/Store/Bms.Store.Application/Extensions/ServiceCollectionExtensions.cs` 的 `AddApplicationServices` 方法中，找到 `services.AddScoped<IInventoryLogAppService, InventoryLogAppService>();` 这一行（约第 54 行），在其下方追加：

```csharp
        services.AddScoped<IInboundAppService, InboundAppService>();
```

注：FluentValidation 通过 `AddValidatorsFromAssemblyContaining<StoreCreateDtoValidator>()` 自动注册，`InboundCreateDtoValidator` 无需手工注册。

- [ ] **Step 2: 验证编译**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED

- [ ] **Step 3: Commit**

```bash
git add src/Services/Store/Bms.Store.Application/Extensions/ServiceCollectionExtensions.cs
git commit -m "feat: 注册 IInboundAppService 到 DI 容器"
```

---

### Task 11: InventoryLogsController 新增 /inbound 端点

**Files:**
- Modify: `src/Services/Store/Bms.Store.Api/Controllers/InventoryLogsController.cs`

- [ ] **Step 1: 注入 `IInboundAppService` 并新增 `[HttpPost("inbound")]` 端点**

修改 `src/Services/Store/Bms.Store.Api/Controllers/InventoryLogsController.cs`：

1) 在 `using` 区追加（若已有则跳过）：
```csharp
using Bms.Store.Application.Dtos.Inventories;
```

2) 在构造函数注入 `IInboundAppService`，替换现有构造函数与字段：
```csharp
    private readonly IInventoryLogAppService _appService;
    private readonly IInboundAppService _inboundAppService;

    public InventoryLogsController(IInventoryLogAppService appService, IInboundAppService inboundAppService)
    {
        _appService = appService;
        _inboundAppService = inboundAppService;
    }
```

3) 在 `BatchDelete` 方法之后（class 闭合 `}` 之前）追加：
```csharp
    /// <summary>
    /// 入库（语义化端点，事务内同步维护 InventoryLog + InventoryBatch + Inventory 三表）
    /// </summary>
    [HttpPost("inbound")]
    public async Task<ApiResponseDto<InventoryLogDto>> Inbound([FromBody] InboundCreateDto dto)
        => await _inboundAppService.CreateAsync(dto);
```

注：`[HttpPost("inbound")]` 与现有 `[HttpPost("batch")]` 路由不冲突，且与 `[HttpPost]`（无模板）共存于同一控制器。路由匹配时 ASP.NET Core 优先匹配带模板的路由。

- [ ] **Step 2: 验证编译**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED

- [ ] **Step 3: Commit**

```bash
git add src/Services/Store/Bms.Store.Api/Controllers/InventoryLogsController.cs
git commit -m "feat: InventoryLogsController 新增 /inbound 语义化入库端点"
```

---

### Task 12: 重写 InventoryLogAppService.GetPagedListAsync 查询逻辑

**Files:**
- Modify: `src/Services/Store/Bms.Store.Application/Services/InventoryLogAppService.cs`

- [ ] **Step 1: 改写 `GetPagedListAsync` 方法，新增 ProductName/StartDate/EndDate 过滤与 Include 导航属性**

替换 `src/Services/Store/Bms.Store.Application/Services/InventoryLogAppService.cs` 中 `GetPagedListAsync` 方法整体（约第 35-70 行）为：

```csharp
    /// <summary>
    /// 获取库存流水分页列表
    /// 查询时 Include Product 与 Supplier 导航属性，内存中填充显示字段
    /// </summary>
    public async Task<ApiResponseDto<PagedResponseDto<InventoryLogDto>>> GetPagedListAsync(InventoryLogQueryDto query)
    {
        if (!_currentUser.TenantId.HasValue || !_currentUser.StoreId.HasValue)
            return ApiResponseDto<PagedResponseDto<InventoryLogDto>>.Fail("无法确定当前租户或门店", 401);

        var tenantId = _currentUser.TenantId.Value;
        var storeId = _currentUser.StoreId.Value;
        var queryable = _dbContext.InventoryLogs
            .Include(l => l.Product)
            .Include(l => l.Supplier)
            .Where(l => l.TenantId == tenantId && l.StoreId == storeId);

        if (query.ProductId.HasValue)
            queryable = queryable.Where(l => l.ProductId == query.ProductId.Value);
        if (query.Type.HasValue)
            queryable = queryable.Where(l => l.Type == query.Type.Value);
        if (query.SourceType.HasValue)
            queryable = queryable.Where(l => l.SourceType == query.SourceType.Value);
        if (!string.IsNullOrWhiteSpace(query.ProductName))
            queryable = queryable.Where(l => l.Product != null && l.Product.Name.Contains(query.ProductName));
        if (query.StartDate.HasValue)
            queryable = queryable.Where(l => l.CreatedTime >= query.StartDate.Value);
        if (query.EndDate.HasValue)
            // EndDate 含当日：过滤条件为 CreatedTime <= EndDate 当天 23:59:59
            queryable = queryable.Where(l => l.CreatedTime <= query.EndDate.Value.Date.AddDays(1).AddTicks(-1));

        var total = await queryable.CountAsync();
        var items = await queryable
            .OrderByDescending(l => l.CreatedTime)
            .Skip((query.PageIndex - 1) * query.PageSize)
            .Take(query.PageSize)
            .ToListAsync();

        // 内存中投影到 DTO，填充显示字段（Adapt 会自动映射同名字段，手工补齐关联字段）
        var dtoList = items.Select(l => new InventoryLogDto
        {
            Id = l.Id,
            ProductId = l.ProductId,
            Type = l.Type,
            SourceType = l.SourceType,
            SupplierId = l.SupplierId,
            UnitPrice = l.UnitPrice,
            Quantity = l.Quantity,
            BeforeQuantity = l.BeforeQuantity,
            AfterQuantity = l.AfterQuantity,
            BatchNo = l.BatchNo,
            ExpirationDate = l.ExpirationDate,
            RelatedId = l.RelatedId,
            Remark = l.Remark,
            CreatedAt = l.CreatedTime,
            UpdatedAt = l.UpdatedTime,
            ProductName = l.Product?.Name,
            ProductCode = l.Product?.Code,
            SupplierName = l.Supplier?.Name,
            OperatorName = l.OperatorName
        }).ToList();

        var result = new PagedResponseDto<InventoryLogDto>
        {
            List = dtoList,
            Total = total,
            PageIndex = query.PageIndex,
            PageSize = query.PageSize
        };
        return ApiResponseDto<PagedResponseDto<InventoryLogDto>>.Ok(result);
    }
```

注：
- 现有 `ProductId` 过滤器保留不动
- `EndDate` 处理为当天 23:59:59，符合"含当日"语义
- 改用手工投影替代 `Adapt<List<InventoryLogDto>>()`，因 Mapster 不会自动填充 `ProductName/ProductCode/SupplierName` 关联字段

- [ ] **Step 2: 验证编译**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED

- [ ] **Step 3: Commit**

```bash
git add src/Services/Store/Bms.Store.Application/Services/InventoryLogAppService.cs
git commit -m "feat: InventoryLogAppService.GetPagedListAsync 改写为 Include+显示字段填充"
```

---

### Task 13: 创建 ProductOptionDto 并新增 IProductAppService.GetOptionsAsync

**Files:**
- Create: `src/Services/Store/Bms.Store.Application/Dtos/Products/ProductOptionDto.cs`
- Modify: `src/Services/Store/Bms.Store.Application/Services/IProductAppService.cs`
- Modify: `src/Services/Store/Bms.Store.Application/Services/ProductAppService.cs`

- [ ] **Step 1: 创建 `ProductOptionDto`**

创建 `src/Services/Store/Bms.Store.Application/Dtos/Products/ProductOptionDto.cs`：

```csharp
namespace Bms.Store.Application.Dtos.Products;

/// <summary>
/// 商品轻量选项 DTO（用于下拉选择场景，不分页）
/// </summary>
public class ProductOptionDto
{
    /// <summary>商品ID</summary>
    public long Id { get; set; }

    /// <summary>商品名称</summary>
    public string Name { get; set; } = string.Empty;

    /// <summary>商品编码</summary>
    public string Code { get; set; } = string.Empty;

    /// <summary>单位</summary>
    public string? Unit { get; set; }
}
```

- [ ] **Step 2: 在 `IProductAppService` 接口追加 `GetOptionsAsync` 方法签名**

在 `src/Services/Store/Bms.Store.Application/Services/IProductAppService.cs` 的 `GetSuppliersByProductAsync` 方法签名之后（接口闭合 `}` 之前）追加：

```csharp
    /// <summary>
    /// 获取商品轻量选项列表（不分页，仅返回 Id/Name/Code/Unit，用于下拉选择场景）
    /// </summary>
    Task<ApiResponseDto<List<ProductOptionDto>>> GetOptionsAsync();
```

- [ ] **Step 3: 在 `ProductAppService` 实现 `GetOptionsAsync` 方法**

在 `src/Services/Store/Bms.Store.Application/Services/ProductAppService.cs` 的 `GetSuppliersByProductAsync` 方法之后（`FillSubTableFieldsAsync` 之前）追加：

```csharp
    /// <summary>
    /// 获取商品轻量选项列表（不分页，仅返回 Id/Name/Code/Unit）
    /// 仅按 TenantId + StoreId 过滤，排除已软删除商品
    /// </summary>
    public async Task<ApiResponseDto<List<ProductOptionDto>>> GetOptionsAsync()
    {
        if (!_currentUser.TenantId.HasValue)
            return ApiResponseDto<List<ProductOptionDto>>.Fail("无法确定当前租户", 401);

        var tenantId = _currentUser.TenantId.Value;
        var storeId = _currentUser.StoreId ?? 0;

        var options = await _dbContext.Products
            .Where(p => !p.IsDeleted && p.TenantId == tenantId && p.StoreId == storeId)
            .OrderBy(p => p.Name)
            .Select(p => new ProductOptionDto
            {
                Id = p.Id,
                Name = p.Name,
                Code = p.Code,
                Unit = p.Unit
            })
            .ToListAsync();

        return ApiResponseDto<List<ProductOptionDto>>.Ok(options);
    }
```

- [ ] **Step 4: 验证编译**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED

- [ ] **Step 5: Commit**

```bash
git add src/Services/Store/Bms.Store.Application/Dtos/Products/ProductOptionDto.cs \
        src/Services/Store/Bms.Store.Application/Services/IProductAppService.cs \
        src/Services/Store/Bms.Store.Application/Services/ProductAppService.cs
git commit -m "feat: 新增 ProductOptionDto 与 ProductAppService.GetOptionsAsync 轻量端点"
```

---

### Task 14: 创建 SupplierOptionDto 并新增 ISupplierAppService.GetOptionsAsync

**Files:**
- Create: `src/Services/Store/Bms.Store.Application/Dtos/Suppliers/SupplierOptionDto.cs`
- Modify: `src/Services/Store/Bms.Store.Application/Services/ISupplierAppService.cs`
- Modify: `src/Services/Store/Bms.Store.Application/Services/SupplierAppService.cs`

- [ ] **Step 1: 创建 `SupplierOptionDto`**

创建 `src/Services/Store/Bms.Store.Application/Dtos/Suppliers/SupplierOptionDto.cs`：

```csharp
namespace Bms.Store.Application.Dtos.Suppliers;

/// <summary>
/// 供应商轻量选项 DTO（用于下拉选择场景，不分页）
/// </summary>
public class SupplierOptionDto
{
    /// <summary>供应商ID</summary>
    public long Id { get; set; }

    /// <summary>供应商名称</summary>
    public string Name { get; set; } = string.Empty;
}
```

- [ ] **Step 2: 在 `ISupplierAppService` 接口追加 `GetOptionsAsync` 方法签名**

在 `src/Services/Store/Bms.Store.Application/Services/ISupplierAppService.cs` 的 `GetProductsBySupplierAsync` 方法签名之后（接口闭合 `}` 之前）追加：

```csharp
    /// <summary>
    /// 获取供应商轻量选项列表（不分页，仅返回 Id/Name，用于下拉选择场景）
    /// </summary>
    Task<ApiResponseDto<List<SupplierOptionDto>>> GetOptionsAsync();
```

- [ ] **Step 3: 在 `SupplierAppService` 实现 `GetOptionsAsync` 方法**

在 `src/Services/Store/Bms.Store.Application/Services/SupplierAppService.cs` 的 `GetProductsBySupplierAsync` 方法之后追加：

```csharp
    /// <summary>
    /// 获取供应商轻量选项列表（不分页，仅返回 Id/Name）
    /// 仅按 TenantId + StoreId 过滤，排除已软删除供应商
    /// </summary>
    public async Task<ApiResponseDto<List<SupplierOptionDto>>> GetOptionsAsync()
    {
        if (!_currentUser.TenantId.HasValue)
            return ApiResponseDto<List<SupplierOptionDto>>.Fail("无法确定当前租户", 401);

        var tenantId = _currentUser.TenantId.Value;
        var storeId = _currentUser.StoreId ?? 0;

        var options = await _dbContext.Suppliers
            .Where(s => !s.IsDeleted && s.TenantId == tenantId && s.StoreId == storeId)
            .OrderBy(s => s.Name)
            .Select(s => new SupplierOptionDto
            {
                Id = s.Id,
                Name = s.Name
            })
            .ToListAsync();

        return ApiResponseDto<List<SupplierOptionDto>>.Ok(options);
    }
```

- [ ] **Step 4: 验证编译**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED

- [ ] **Step 5: Commit**

```bash
git add src/Services/Store/Bms.Store.Application/Dtos/Suppliers/SupplierOptionDto.cs \
        src/Services/Store/Bms.Store.Application/Services/ISupplierAppService.cs \
        src/Services/Store/Bms.Store.Application/Services/SupplierAppService.cs
git commit -m "feat: 新增 SupplierOptionDto 与 SupplierAppService.GetOptionsAsync 轻量端点"
```

---

### Task 15: ProductsController 新增 /options 端点

**Files:**
- Modify: `src/Services/Store/Bms.Store.Api/Controllers/ProductsController.cs`

- [ ] **Step 1: 在 `ProductsController` 追加 `[HttpGet("options")]` 端点**

在 `src/Services/Store/Bms.Store.Api/Controllers/ProductsController.cs` 的 `GetSuppliersByProduct` 方法之后（class 闭合 `}` 之前）追加：

```csharp
    /// <summary>
    /// 获取商品轻量选项列表（不分页，用于下拉选择场景）
    /// </summary>
    [HttpGet("options")]
    public async Task<ApiResponseDto<List<ProductOptionDto>>> GetOptions()
        => await _productAppService.GetOptionsAsync();
```

注：路由前缀为 `api/store/product/[controller]`，故完整路径为 `GET /api/store/product/products/options`。前端 `storeRequest` 的 `API_BASE` 为 `/api/store`，相对路径为 `/product/products/options`。

- [ ] **Step 2: 验证编译**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED

- [ ] **Step 3: Commit**

```bash
git add src/Services/Store/Bms.Store.Api/Controllers/ProductsController.cs
git commit -m "feat: ProductsController 新增 /options 轻量端点"
```

---

### Task 16: SuppliersController 新增 /options 端点

**Files:**
- Modify: `src/Services/Store/Bms.Store.Api/Controllers/SuppliersController.cs`

- [ ] **Step 1: 在 `SuppliersController` 追加 `[HttpGet("options")]` 端点**

在 `src/Services/Store/Bms.Store.Api/Controllers/SuppliersController.cs` 的 `GetProductsBySupplier` 方法之后（class 闭合 `}` 之前）追加：

```csharp
    /// <summary>
    /// 获取供应商轻量选项列表（不分页，用于下拉选择场景）
    /// </summary>
    [HttpGet("options")]
    public async Task<ApiResponseDto<List<SupplierOptionDto>>> GetOptions()
        => await _appService.GetOptionsAsync();
```

注：路由前缀为 `api/store/[controller]`，故完整路径为 `GET /api/store/suppliers/options`。前端相对路径为 `/suppliers/options`。

- [ ] **Step 2: 验证编译**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED

- [ ] **Step 3: Commit**

```bash
git add src/Services/Store/Bms.Store.Api/Controllers/SuppliersController.cs
git commit -m "feat: SuppliersController 新增 /options 轻量端点"
```

---

## Chunk 3: 前端 API 替换

### Task 17: 更新 types.ts 移除后端不返回的字段

**Files:**
- Modify: `src/Admin/mes-admin/src/api/inventory-ops/types.ts`

- [ ] **Step 1: 从 `InventoryLog` 接口移除 `productionDate?` 与 `shelfLifeDays?` 字段**

在 `src/Admin/mes-admin/src/api/inventory-ops/types.ts` 中找到 `InventoryLog` 接口的以下两行（约第 65-68 行）：

```typescript
  /** 生产日期 */
  productionDate?: string
  /** 保质期天数 */
  shelfLifeDays?: number
```

整段删除这两行（含注释）。

**理由：** 后端 `InventoryLogDto` 不返回这两个字段，它们仅存在于 `InventoryBatch` 实体。前端 `InventoryLog` 接口保留这两个字段会导致类型与实际响应不匹配，且 `inbound/index.vue` 不依赖这两个字段（入库表单填写后直接 POST，不回显到列表）。

**保持不变的字段：**
- `InboundSourceType`（`1 | 2 | 3 | 4`）保持不变，与前端表单暴露的 4 个选项一致
- `InboundRequest` 接口保持不变，字段已与后端 `InboundCreateDto` 对齐
- `InventoryLogQuery` 接口保持不变，已有 `productName?` / `startDate?` / `endDate?` 字段
- `productName` / `productCode?` / `supplierName?` / `operatorName?` 已存在，保留不变

- [ ] **Step 2: 验证 TypeScript 编译**

Run: `cd src/Admin/mes-admin && npx tsc --noEmit`
Expected: 无错误

**排查说明：** 此改动仅影响 `InventoryLog` 响应类型。以下引用**不受影响**，无需处理：
- `inbound/index.vue` 中 `formData.productionDate` / `formData.shelfLifeDays`：表单内部状态，类型由 `formData` 自身定义，不依赖 `InventoryLog` 接口
- `inbound/index.vue` 第 460-461 行构建 `InboundRequest` 时的 `productionDate:` / `shelfLifeDays:` 字段：`InboundRequest` 接口保留这两个字段（后端 `InboundCreateDto` 有对应字段）
- `api/inventory/types.ts` 中 `InventoryBatch` 接口的同名字段：属于另一个 API 模块，对应后端 `InventoryBatch` 实体，不在本期改动范围

若 `tsc --noEmit` 报错，需排查是否有代码通过 `InventoryLog` 类型的变量访问 `.productionDate` 或 `.shelfLifeDays`（例如 `log.productionDate`），删除该访问即可。

- [ ] **Step 3: Commit**

```bash
git add src/Admin/mes-admin/src/api/inventory-ops/types.ts
git commit -m "refactor: InventoryLog 类型移除后端不返回的 productionDate/shelfLifeDays 字段"
```

---

### Task 18: 重写 inventory-ops/index.ts 替换 Mock 为真实 HTTP 调用

**Files:**
- Modify: `src/Admin/mes-admin/src/api/inventory-ops/index.ts`

- [ ] **Step 1: 排查 inbound/outbound 页面对 Mock 数据的依赖**

Run: `cd src/Admin/mes-admin && grep -rn "from.*inventory-ops" src/views/store/product/inbound/ src/views/store/product/outbound/`

确认两个页面仅通过导出的函数访问 API（不直接引用 Mock 变量）。若页面直接 import 了 `mockInventoryLogs` 等内部变量，需先改为调用函数。

- [ ] **Step 2: 整体重写 `src/Admin/mes-admin/src/api/inventory-ops/index.ts`**

将 `src/Admin/mes-admin/src/api/inventory-ops/index.ts` 整体替换为以下内容：

```typescript
// 入库/出库操作 - API 服务
// 入库相关函数对接后端真实端点；出库暂保留 Mock，下一期替换
import type {
  InventoryLog,
  InventoryLogQuery,
  InboundRequest,
  OutboundRequest,
  InventoryOpType,
  InboundSourceType,
  PagedResponse
} from './types'
import { request } from '../shared/storeRequest'

// 导出类型供外部使用
export type {
  InventoryLog,
  InventoryLogQuery,
  InboundRequest,
  OutboundRequest,
  InventoryOpType,
  InboundSourceType,
  PagedResponse
}

// ==================== API 函数 ====================

/**
 * 获取库存操作日志分页列表
 * 对接后端：GET /api/store/inventorylogs
 * @param query 查询参数
 * @returns 分页日志列表
 */
export async function getInventoryLogList(query?: InventoryLogQuery): Promise<PagedResponse<InventoryLog>> {
  const params = new URLSearchParams()
  if (query?.productName) params.append('productName', query.productName)
  if (query?.type !== undefined) params.append('type', String(query.type))
  if (query?.sourceType !== undefined) params.append('sourceType', String(query.sourceType))
  if (query?.startDate) params.append('startDate', query.startDate)
  if (query?.endDate) params.append('endDate', query.endDate)
  params.append('pageIndex', String(query?.pageIndex || 1))
  params.append('pageSize', String(query?.pageSize || 20))
  return request<PagedResponse<InventoryLog>>(`/inventorylogs?${params}`)
}

/**
 * 入库操作（事务内同步维护 InventoryLog + InventoryBatch + Inventory 三表）
 * 对接后端：POST /api/store/inventorylogs/inbound
 * @param data 入库请求
 * @returns 创建后的库存日志（含商品/供应商/操作人显示字段）
 */
export async function createInbound(data: InboundRequest): Promise<InventoryLog> {
  return request<InventoryLog>('/inventorylogs/inbound', {
    method: 'POST',
    body: JSON.stringify(data)
  })
}

/**
 * 出库操作（Mock 实现，下一期做出库时替换为 POST /inventorylogs/outbound）
 * @param data 出库请求
 * @returns 创建后的库存日志
 */
export async function createOutbound(data: OutboundRequest): Promise<InventoryLog> {
  // Mock 实现：出库功能下一期对接后端
  // 不依赖共享 Mock 状态，仅返回合成的日志对象
  return {
    id: 0,
    productId: data.productId,
    productName: '',
    type: 2,
    quantity: -data.quantity,
    beforeQuantity: 0,
    afterQuantity: 0,
    remark: data.remark,
    createdAt: new Date().toISOString(),
    operatorName: '当前用户'
  }
}

/**
 * 获取商品轻量选项列表（用于下拉选择，不分页）
 * 对接后端：GET /api/store/product/products/options
 * 注意：ProductsController 路由前缀为 api/store/product/[controller]，与其他控制器不同
 * @returns 商品选项数组
 */
export async function getProductOptions(): Promise<{ id: number; name: string; code: string; unit: string }[]> {
  return request<{ id: number; name: string; code: string; unit: string }[]>('/product/products/options')
}

/**
 * 获取供应商轻量选项列表（用于下拉选择，不分页）
 * 对接后端：GET /api/store/suppliers/options
 * @returns 供应商选项数组
 */
export async function getSupplierOptions(): Promise<{ id: number; name: string }[]> {
  return request<{ id: number; name: string }[]>('/suppliers/options')
}

/**
 * 获取商品当前库存
 * 对接后端：GET /api/store/inventories?productId={id}&pageSize=1
 * @param productId 商品ID
 * @returns 当前库存数量（无记录返回 0）
 */
export async function getProductStock(productId: number): Promise<number> {
  const params = new URLSearchParams()
  params.append('productId', String(productId))
  params.append('pageSize', '1')
  const result = await request<PagedResponse<{ id: number; productId: number; quantity: number }> | null>(`/inventories?${params}`)
  // 无记录时后端返回空列表或 null，统一返回 0
  if (!result || !result.list || result.list.length === 0) return 0
  return result.list[0].quantity
}

/**
 * 入库来源类型标签映射
 */
export const inboundSourceTypeMap: Record<InboundSourceType, string> = {
  1: '采购入库',
  2: '退货入库',
  3: '盘点入库',
  4: '调拨入库'
}

/**
 * 库存操作类型标签映射
 */
export const inventoryOpTypeMap: Record<InventoryOpType, string> = {
  1: '入库',
  2: '出库',
  3: '盘点',
  4: '调拨'
}
```

**关键设计点说明：**
- 删除全部 Mock 数据（`mockStockMap` / `mockInventoryLogs` / `mockProductOptions` / `mockSupplierOptions` / `logIdCounter`）与所有 `setTimeout` 模拟延迟
- `getInventoryLogList`：`GET /inventorylogs` + URLSearchParams，与 `product/index.ts` 的 `getProducts` 风格一致
- `createInbound`：`POST /inventorylogs/inbound`，body 为 `InboundRequest`（已与后端 `InboundCreateDto` 字段对齐）
- `createOutbound`：保留 Mock 但自包含（不依赖共享 Mock 状态），返回合成的 `InventoryLog`。出库页面本期不动，下一期替换为 `POST /inventorylogs/outbound`
- `getProductOptions`：`GET /product/products/options`（注意路径含 `/product` 前缀，因 `ProductsController` 路由前缀为 `api/store/product/[controller]`）
- `getSupplierOptions`：`GET /suppliers/options`
- `getProductStock`：`GET /inventories?productId={id}&pageSize=1`，取 `list[0].quantity`；无记录返回 0
- `inboundSourceTypeMap` / `inventoryOpTypeMap` 保持不变

- [ ] **Step 3: 验证 TypeScript 编译**

Run: `cd src/Admin/mes-admin && npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 4: Commit**

```bash
git add src/Admin/mes-admin/src/api/inventory-ops/index.ts
git commit -m "feat: 入库 API 替换 Mock 为真实 HTTP 调用，出库暂保留 Mock"
```

---

### Task 19: 前端整体构建验证

**Files:**
- 无文件修改（仅验证）

- [ ] **Step 1: 运行前端构建**

Run: `cd src/Admin/mes-admin && npm run build`
Expected: BUILD SUCCEEDED（无 TypeScript 错误，无 Vite 构建错误）

**常见失败场景与排查：**
1. **`InventoryLog` 类型的变量访问 `.productionDate` / `.shelfLifeDays`**：Task 17 已从 `InventoryLog` 接口移除这两个字段。若代码中有 `const log: InventoryLog = ...; log.productionDate` 之类的访问会报错。注意 `formData.productionDate`（表单状态）和 `InboundRequest` 构建时的 `productionDate:` 字段**不受影响**。排查：`grep -rn "\.productionDate\|\.shelfLifeDays" src/views/store/product/`，仅删除通过 `InventoryLog` 类型变量访问的代码。
2. **`createOutbound` 返回类型不匹配**：Mock `createOutbound` 返回的 `InventoryLog` 缺少 `productName` 必填字段会报错。已在 Step 2 中用 `productName: ''` 填充，确保类型完整。
3. **`getProductStock` 返回类型推断失败**：若 `request<...>` 泛型参数与实际响应不匹配会报错。确认 `PagedResponse` 字段名（`list` / `total` / `pageIndex` / `pageSize`）与后端 `PagedResponseDto` 一致。

- [ ] **Step 2: 运行后端整体构建（最终回归验证）**

Run: `dotnet build Bms.sln`
Expected: BUILD SUCCEEDED

确认 Chunk 1-2 的所有后端改动整体编译通过，无遗漏的引用错误。

- [ ] **Step 3: Commit（仅当有未提交的修复时）**

若 Step 1-2 因排查修复了额外文件，提交修复（限定前端源码目录，遵循 git-workflow 规则）：

```bash
git add src/Admin/mes-admin/src/
git commit -m "fix: 前端构建排查修复"
```

若 Step 1-2 均一次通过，无需提交，跳过此步。

---

## 计划完成

完成全部 19 个任务后，入库功能完整闭环：

- 后端：`POST /api/store/inventorylogs/inbound` 语义化端点（事务内三表写入）、`GET /api/store/product/products/options`、`GET /api/store/suppliers/options` 轻量端点、`InventoryLogDto` 显示字段补齐
- 前端：`inventory-ops/index.ts` 6 个函数中 5 个改为真实 HTTP 调用，`createOutbound` 保留 Mock 待下一期
- 数据库：新增 `InventoryLogs.OperatorName` 列、`InventoryLog.Supplier` 导航属性外键

**下一期工作：** 出库功能（`POST /inventorylogs/outbound` 端点 + `createOutbound` 替换为真实 HTTP + `outbound/index.vue` 联调）。
